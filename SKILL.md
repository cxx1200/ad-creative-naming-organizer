---
name: ad-creative-naming-organizer
description: >
  使用飞书 Base 对广告创意素材做命名规则预览与执行整理：按项目匹配命名模板，扫描文件夹媒体文件，
  写入预览整理表，人工确认后复制存档并生成改名副本。适用于广告优化师整理共享盘视频/图片/音频素材；
  不用于素材制作、翻译、删除或清理。
metadata:
  short-description: 广告创意素材命名预览与执行整理
---

# 广告创意素材命名整理器

这个 skill 用固定飞书 Base 管理命名规则、模板、预览记录和人工修正记忆。脚本只做确定性操作：读取 Base、扫描文件、解析模板、复制文件；是否执行永远由优化师在预览表确认后决定。

## 铁律

- `preview` 只扫描文件并写 Base 预览记录，不移动、不改名、不删除本地文件。
- `apply` 是唯一本地写入动作，只有优化师明确确认后才能运行。
- 执行时不覆盖源文件；先复制原文件到 `_原始存档_*`，生成改名副本后，从源目录主目录移除原文件，避免新旧文件名重复。
- 所有飞书 Base 调用固定使用 `--as user`，脚本内部已处理。
- 一次任务只处理一个项目和一个文件夹；只扫描根目录，不递归。
- 角色权限只是团队约定，不写身份校验；实际权限由飞书 Base 权限控制。

## 首次准备

第一次使用或表结构缺失时运行：

```powershell
python C:\Users\rose\.codex\skills\ad-creative-naming-organizer\scripts\organizer.py ensure-schema
```

脚本会自动确保 `命名模板库`、`命名修正记忆` 存在，并补齐 `预览整理表` 与 `命名模板库` 的归一化字段。需要 `lark-cli >= 1.0.93`；本次运行会刷新本机 schema 缓存，之后预览/执行默认读取缓存。表结构见 [references/base-schema.md](references/base-schema.md)。

## 生成预览

```powershell
python C:\Users\rose\.codex\skills\ad-creative-naming-organizer\scripts\organizer.py preview `
  --project <项目名称> `
  --folder <绝对路径>
```

脚本会：

1. 读取该项目 `命名模板库` 中所有 `启用` 模板；若没有启用模板则停止。
2. 按 `优先级` 从小到大排序，并收集该项目的 `归一化规则`。
3. 只扫描目标文件夹根目录媒体文件，并排除隐藏文件与工具产物。
4. 对每个文件名先按归一化规则整理，再按模板判断是否命中；任一启用模板命中即保持原名。
5. 未命中时用最高优先级启用模板生成建议名，并在需要比例/尺寸/分辨率时读取媒体元数据。
6. 将结果幂等写入 `预览整理表`，不删除旧记录；`已执行` 的历史记录不再参与幂等匹配。

优化师在 `预览整理表` 检查并修改 `建议新文件名`。脚本会保留人工修改后的建议名，直到执行。

## 确认后执行

只有优化师明确说“确认执行”后运行：

```powershell
python C:\Users\rose\.codex\skills\ad-creative-naming-organizer\scripts\organizer.py apply `
  --project <项目名称> `
  --folder <绝对路径>
```

脚本会重新读取预览表，最终以当前 `建议新文件名` 为准；对人工修改过的记录先写 `命名修正记忆`，再在源目录内创建 `_原始存档_YYYYMMDD_HHMMSS`，复制原文件存档并复制改名副本回主目录，然后删除主目录中的原文件。命中文件不移动、不改名、不复制；目标名冲突时追加 `_02`、`_03`。

执行后在对话中汇报扫描数、命中数、改名数、记忆修正数、冲突数和存档路径。

如果本次执行新增了 `命名修正记忆`，脚本会自动从该项目的修正记忆归纳通用规则，并补充到 `命名模板库`。新增记录默认 `是否启用=待确认`，不会修改已有启用模板；若规则与现有归一化规则冲突，会在 `备注` 中说明并保持待确认。自动补充只覆盖可归纳的通用模式，不会简单复制单条记忆。

## 模板与字段取值字典

- `命名模板` 使用 `{字段}` 表示必填，`{字段?}` 表示可选，例如 `{项目}_{素材ID}_{主题?}_{日期}_{比例?}_{语言?}`。
- `字段取值字典` 每行一条 `字段=值1,值2,值3`，也兼容 `字段: 值1,值2,值3`。
- 可选字段缺失时，脚本会连同前一个分隔符一起省略。
- `字段取值字典` 中可加一行 `比例格式: 三码`，支持 `三码`（默认）、`x` 或 `冒号`；影响媒体尺寸识别后的比例输出和比例标准化。
- `比例`、`尺寸`、`分辨率` 无法从原文件名得到时，从媒体文件读取；仍无法读取则标为 `【待确认】`。
- `归一化说明` 供优化师用自然语言描述规则，例如“结尾如果是 `比例-语言`，则改成 `语言_比例`”。
- `归一化规则` 每行一条 `匹配正则 => 替换模板`，脚本按模板优先级和行顺序在匹配前执行。自然语言说明与大模型生成/校验机器规则，执行时以机器规则为准。
